TODOS
1) add unit testing
2) add branch to build a web app with flask
3) add indeed USA integration + argv logic
4) make a better dictionary of keywords or a txt file list


LIST OF ERRORS:
1) If i leave "c++" as a value to search with regular expression, it throws an error <re.error: multiple repeat at position 4>, but if I change it to 'c+' it works just fine

#DONE# 3) sistemare line 54 dove, per ora funziona ma la logica non è la migliore, funziona ma è poco leggibile, si aziona se "page" viene definito divefso da zero basato sull'argument, se no va in default su 0

4) sistemare meglio il dictionary delle keyword da ricercare per non avere valori inutili
5) aggiungere diversi dictionaries con altre keywords riguardanti altre professsioni/ambiti lavorativi/skills

09/08
- aggiunto branch 'web_app'

- sistemato vari input, modificato comportamento di dev_mode, aggiunto funzione per creare graph con maptplotlib
===============
08/08
- Aggiunto user input a main, in modo che passi il valore ad extract(page)
- Aggiunto un set in cui viene aggiunta l'id di ogni job che viene analizzato, ma se id è gia presente in set(), allora viene saltato, questo evita che si accumulino valori falsi per job ripetuti piu volte, in quanto su indeed se si forza ad andare su un altra pagina oltre all'ultima, vengono ripetuti gli stessi annunci e quindi verrebbero ri-analizzati dando falsi risultati

Sto pensando di farlo diventare una web-app con flask, tanto la logica sarebbe già quasi completa in python
=====================================
06/08
Aggiunto timing to dev_mode , ma devo Sistemare "ERRORS 2"[FATTO] perche se scelgo una professione con poche offerte o una zona con pochi annunci, le offerte vengono Ri conteggiate le stesse, inquinando i risultati
=====================================
04/08
sistemato e messo in funzione il conteggio dei match e print ad output
dopo aver sistemato iteratore di pagina, ho modificato temporanemamente print_to_file per i total matches
ora funziona restituendo un numero credibile di matches ed i calcoli funzionano bene

=====================================
03/08
sistemato ieratore delle pagine che fa eseguire il comando di ricerca in loop da passare a url

=====================================
31/07
Continuare ad integrare CLI arguments
capire perche la logica funziona in testing.py e non in lookfor.py, in quanto con 2 arguments da una risposta diversa a quello di testing.py

inoltre con il numero di pagine non fa comquneu l'iterazione tra una e l'altra, ma ritorna i risultati della prima, manca qualche passsaggio di var o di loop
=====================================

30/07
TODO
- coontinuare a creare arguments.py per gestire i cli arguments

log:
Funziona, ho iniziato a pulire la formattazione e rimuovere le funzioni non piu utilizzate

sistemato commenti, ed inizio a perfezionare integrazione tra le helper functions
TODO:
_ attivare user input per la ricerca
_ mettere arguments per modificare il comportamento dell'esecuzione (esempio: print result as percents or as total number of matches)
_ add arguments <dev_mode> for the program to:
    - append all the raw text from every listing to the block_of_text.txt file (erase before initiating)
    - add counter to the output file for ALL matches

=====================================
29/07
Non sono riuscito al momento ad iterare ad ogni passaggio della pagina, devo modificare la helper function  di analize in modo che analize chiama soltanto la funzione di conteggio delle parole e log dentro al dictionary
e poi dopo aver parsed tra tutte le pagine, chiamare a parte le due funzioni di modifica e riordino e POI print
se no ogni ovlta riscriveva per ogni listing una nuova list o la appendeva sotto diventando inutile

Ho poi creato un BLOCK of text appendendo tutte le listing per poichiamare analize() su quello  ma mi sono dimenticato che poi analize da solo +1 per listing, che va in conflitto col fatto che ogni listging possa avere piu volte elencata la stessa parola quindi non va bene.

Considerare se fare tutto in live oppure creare diversi file per ogni listing oppure creare un database, ma penso si possa fare benissimo come lo sto facendo tutto in memoria, ma riorganizzando le funzioni e le helper functions calls